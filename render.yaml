services:
  - type: web
    name: douyin-scraper
    runtime: docker
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PUPPETEER_EXECUTABLE_PATH
        value: /usr/bin/chromium
